using UnityEngine;

public class SpellDamage {
    public float damage;
    public Vector3 direction;

    public SpellDamage(float damage, Vector3 direction) {
        this.damage = damage;
        this.direction = direction;
    }
}
